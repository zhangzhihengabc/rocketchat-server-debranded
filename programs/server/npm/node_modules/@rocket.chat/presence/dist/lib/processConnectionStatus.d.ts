import type { IUserSessionConnection } from '@rocket.chat/core-typings';
import { UserStatus } from '@rocket.chat/core-typings';
/**
 * Defines new connection status compared to a previous connection status
 */
export declare const processConnectionStatus: (current: UserStatus, status: UserStatus) => UserStatus;
/**
 * Defines user's status based on presence and connection status
 */
export declare const processStatus: (statusConnection: UserStatus, statusDefault: UserStatus) => UserStatus;
/**
 * Defines user's status and connection status based on user's connections and default status
 */
export declare const processPresenceAndStatus: (userSessions?: IUserSessionConnection[], statusDefault?: UserStatus) => {
    status: UserStatus;
    statusConnection: UserStatus;
};
