import type { IRole } from '@rocket.chat/core-typings';
/**
 * Check if the user has the permission considering the user have the scoped roles
 * @param permission The permission to check
 * @param scopedRoles The roles to be also considered
 * @returns boolean
 */
export declare const usePermissionWithScopedRoles: (permission: string, scopedRoles: IRole['_id'][]) => boolean;
//# sourceMappingURL=usePermissionWithScopedRoles.d.ts.map