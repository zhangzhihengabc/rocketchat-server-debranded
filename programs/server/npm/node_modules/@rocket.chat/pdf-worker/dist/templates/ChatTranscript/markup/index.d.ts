/// <reference types="react" />
import type * as MessageParser from '@rocket.chat/message-parser';
type MarkupProps = {
    tokens: MessageParser.Root;
};
export declare const Markup: ({ tokens }: MarkupProps) => JSX.Element;
export {};
