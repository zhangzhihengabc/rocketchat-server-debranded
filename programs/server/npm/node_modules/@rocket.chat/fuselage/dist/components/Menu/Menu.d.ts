import type { UsePositionOptions } from '@rocket.chat/fuselage-hooks';
import type { ComponentProps, ElementType, ReactNode } from 'react';
import type Box from '../Box';
import { IconButton } from '../Button/IconButton';
type MenuProps = Omit<ComponentProps<typeof IconButton>, 'icon'> & {
    options: {
        [id: string]: {
            type?: 'option' | 'heading' | 'divider';
            label?: ReactNode;
            action?: () => void;
            disabled?: boolean;
        };
    };
    optionWidth?: ComponentProps<typeof Box>['width'];
    placement?: UsePositionOptions['placement'];
    renderItem?: ElementType;
    icon?: ComponentProps<typeof IconButton>['icon'];
    maxHeight?: string | number;
};
export declare const Menu: ({ tiny, mini, small, options, optionWidth, placement, renderItem, maxHeight, icon, ...props }: MenuProps) => JSX.Element;
export {};
//# sourceMappingURL=Menu.d.ts.map