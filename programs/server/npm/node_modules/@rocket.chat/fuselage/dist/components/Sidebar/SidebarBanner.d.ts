import type { ReactNode } from 'react';
type VariantType = 'default' | 'info' | 'success' | 'warning' | 'danger';
type SidebarBannerProps = {
    text?: ReactNode;
    description?: ReactNode;
    onClick?: () => void;
    variant?: VariantType;
    onClose?: () => void;
    children?: ReactNode;
    addon?: ReactNode;
};
export declare const SidebarBanner: ({ text, description, onClick, variant, addon, onClose, children, }: SidebarBannerProps) => JSX.Element;
export {};
//# sourceMappingURL=SidebarBanner.d.ts.map