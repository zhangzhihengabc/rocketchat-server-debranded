import type { ButtonHTMLAttributes } from 'react';
import React from 'react';
type ChipProps = Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'type'> & {
    thumbUrl?: string;
    renderThumb?: (props: {
        url: string;
    }) => React.ReactNode;
    renderDismissSymbol?: () => React.ReactNode;
};
export declare const Chip: {
    ({ children, className, thumbUrl, onClick, onMouseDown, renderThumb, renderDismissSymbol, ...rest }: ChipProps): JSX.Element;
    displayName: string;
};
export {};
//# sourceMappingURL=Chip.d.ts.map