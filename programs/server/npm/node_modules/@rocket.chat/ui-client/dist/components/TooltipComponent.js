import { jsx as _jsx } from "react/jsx-runtime";
import { Tooltip, PositionAnimated, AnimatedVisibility } from '@rocket.chat/fuselage';
import { useRef } from 'react';
export const TooltipComponent = ({ title, anchor }) => {
    const ref = useRef(anchor);
    return (_jsx(PositionAnimated, { anchor: ref, placement: 'top-middle', margin: 8, visible: AnimatedVisibility.UNHIDING, children: _jsx(Tooltip, { children: title }) }));
};
//# sourceMappingURL=TooltipComponent.js.map