import type { Dispatch, MutableRefObject, ReactElement, ReactNode, SetStateAction } from 'react';
type AnchorParams = {
    ref: MutableRefObject<null>;
    toggle: Dispatch<SetStateAction<boolean>>;
    id: string;
};
type TooltipWrapperProps = {
    children: ReactElement | ((props: AnchorParams) => ReactNode);
    text: string;
};
declare const TooltipWrapper: ({ children, text, }: TooltipWrapperProps) => ReactElement;
export default TooltipWrapper;
//# sourceMappingURL=TooltipWrapper.d.ts.map