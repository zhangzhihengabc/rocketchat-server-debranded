import type { Db } from 'mongodb';
export declare class AgendaCronJobs {
    private reservedJobs;
    private scheduler;
    get started(): boolean;
    start(mongo: Db): Promise<void>;
    add(name: string, schedule: string, callback: () => any | Promise<any>): Promise<void>;
    addAtTimestamp(name: string, when: Date, callback: () => any | Promise<any>): Promise<void>;
    remove(name: string): Promise<void>;
    has(jobName: string): Promise<boolean>;
    private reserve;
    private unreserve;
    private define;
}
export declare const cronJobs: AgendaCronJobs;
