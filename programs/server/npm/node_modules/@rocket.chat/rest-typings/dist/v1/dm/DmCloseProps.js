import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const DmClosePropsSchema = {
    type: 'object',
    properties: {
        roomId: {
            type: 'string',
        },
    },
    required: ['roomId'],
    additionalProperties: false,
};
export const isDmCloseProps = ajv.compile(DmClosePropsSchema);
//# sourceMappingURL=DmCloseProps.js.map