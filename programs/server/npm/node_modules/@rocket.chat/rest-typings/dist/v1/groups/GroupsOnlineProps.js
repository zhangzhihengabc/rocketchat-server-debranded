import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const groupsOnlyPropsSchema = {
    type: 'object',
    properties: {
        query: {
            type: 'string',
            nullable: true,
        },
    },
    required: [],
    additionalProperties: false,
};
export const isGroupsOnlineProps = ajv.compile(groupsOnlyPropsSchema);
//# sourceMappingURL=GroupsOnlineProps.js.map