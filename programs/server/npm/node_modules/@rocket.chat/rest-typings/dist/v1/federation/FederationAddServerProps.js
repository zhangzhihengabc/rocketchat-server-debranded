import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const FederationAddServerPropsSchema = {
    type: 'object',
    properties: {
        serverName: {
            type: 'string',
        },
    },
    additionalProperties: false,
    required: ['serverName'],
};
export const isFederationAddServerProps = ajv.compile(FederationAddServerPropsSchema);
//# sourceMappingURL=FederationAddServerProps.js.map