import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
export const isDmDeleteProps = ajv.compile({
    oneOf: [
        {
            type: 'object',
            properties: {
                roomId: {
                    type: 'string',
                },
            },
            required: ['roomId'],
            additionalProperties: false,
        },
        {
            type: 'object',
            properties: {
                username: {
                    type: 'string',
                },
            },
            required: ['username'],
            additionalProperties: false,
        },
    ],
});
//# sourceMappingURL=DmDeleteProps.js.map