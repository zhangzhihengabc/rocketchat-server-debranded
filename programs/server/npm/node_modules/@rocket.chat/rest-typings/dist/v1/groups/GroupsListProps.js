import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const groupsListPropsSchema = {};
export const isGroupsListProps = ajv.compile(groupsListPropsSchema);
//# sourceMappingURL=GroupsListProps.js.map