import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const channelsOnlyPropsSchema = {
    type: 'object',
    properties: {
        query: {
            type: 'string',
            nullable: true,
        },
    },
    required: [],
    additionalProperties: false,
};
export const isChannelsOnlineProps = ajv.compile(channelsOnlyPropsSchema);
//# sourceMappingURL=ChannelsOnlineProps.js.map