import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const UsersListTeamsParamsGetSchema = {
    type: 'object',
    properties: {
        userId: {
            type: 'string',
        },
    },
    required: ['userId'],
    additionalProperties: false,
};
export const isUsersListTeamsProps = ajv.compile(UsersListTeamsParamsGetSchema);
//# sourceMappingURL=UsersListTeamsParamsGET.js.map