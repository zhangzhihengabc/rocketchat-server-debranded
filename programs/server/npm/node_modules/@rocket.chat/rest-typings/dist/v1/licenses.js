import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const licensesAddPropsSchema = {
    type: 'object',
    properties: {
        license: {
            type: 'string',
        },
    },
    required: ['license'],
    additionalProperties: false,
};
export const isLicensesAddProps = ajv.compile(licensesAddPropsSchema);
const licensesInfoPropsSchema = {
    type: 'object',
    properties: {
        loadValues: {
            type: 'boolean',
        },
    },
    required: [],
    additionalProperties: false,
};
export const isLicensesInfoProps = ajv.compile(licensesInfoPropsSchema);
//# sourceMappingURL=licenses.js.map