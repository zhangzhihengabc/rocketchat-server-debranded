import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const DmLeavePropsSchema = {
    oneOf: [
        {
            type: 'object',
            properties: {
                roomId: {
                    type: 'string',
                },
            },
            required: ['roomId'],
            additionalProperties: false,
        },
        {
            type: 'object',
            properties: {
                roomName: {
                    type: 'string',
                },
            },
            required: ['roomName'],
            additionalProperties: false,
        },
    ],
};
export const isDmLeaveProps = ajv.compile(DmLeavePropsSchema);
//# sourceMappingURL=DmLeaveProps.js.map