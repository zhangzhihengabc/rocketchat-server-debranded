import Ajv from 'ajv';
import { withGroupBaseProperties } from './BaseProps';
const ajv = new Ajv({
    coerceTypes: true,
});
const groupsSetDescriptionPropsSchema = withGroupBaseProperties({
    description: {
        type: 'string',
    },
}, ['description']);
export const isGroupsSetDescriptionProps = ajv.compile(groupsSetDescriptionPropsSchema);
//# sourceMappingURL=GroupsSetDescriptionProps.js.map