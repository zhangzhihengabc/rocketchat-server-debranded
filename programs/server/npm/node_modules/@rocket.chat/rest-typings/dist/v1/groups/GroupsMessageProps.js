import Ajv from 'ajv';
import { withGroupBaseProperties } from './BaseProps';
const ajv = new Ajv({
    coerceTypes: true,
});
const GroupsMessagePropsSchema = withGroupBaseProperties({
    count: {
        type: 'number',
        nullable: true,
    },
    offset: {
        type: 'number',
        nullable: true,
    },
    sort: {
        type: 'string',
        nullable: true,
    },
    query: {
        type: 'string',
        nullable: true,
    },
});
export const isGroupsMessageProps = ajv.compile(GroupsMessagePropsSchema);
//# sourceMappingURL=GroupsMessageProps.js.map