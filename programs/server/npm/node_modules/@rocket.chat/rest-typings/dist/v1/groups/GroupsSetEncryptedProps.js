import Ajv from 'ajv';
import { withGroupBaseProperties } from './BaseProps';
const ajv = new Ajv({
    coerceTypes: true,
});
const groupsSetEncryptedPropsSchema = withGroupBaseProperties({
    encrypted: {
        type: 'boolean',
    },
}, ['encrypted']);
export const isGroupsSetEncryptedProps = ajv.compile(groupsSetEncryptedPropsSchema);
//# sourceMappingURL=GroupsSetEncryptedProps.js.map