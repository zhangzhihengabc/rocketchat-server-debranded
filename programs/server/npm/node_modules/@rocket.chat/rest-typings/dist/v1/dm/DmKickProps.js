import Ajv from 'ajv';
const ajv = new Ajv({
    coerceTypes: true,
});
const DmKickPropsSchema = {
    type: 'object',
    properties: {
        roomId: {
            type: 'string',
        },
        userId: {
            type: 'string',
        },
    },
    required: ['roomId', 'userId'],
    additionalProperties: false,
};
export const isDmKickProps = ajv.compile(DmKickPropsSchema);
//# sourceMappingURL=DmKickProps.js.map