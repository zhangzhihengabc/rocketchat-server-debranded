import { ajv } from '../Ajv';
const archiveReportPropsSchema = {
    type: 'object',
    properties: {
        userId: {
            type: 'string',
        },
        msgId: {
            type: 'string',
        },
        reason: {
            type: 'string',
            nullable: true,
        },
        action: {
            type: 'string',
            nullable: true,
        },
    },
    oneOf: [{ required: ['msgId'] }, { required: ['userId'] }],
    additionalProperties: false,
};
export const isArchiveReportProps = ajv.compile(archiveReportPropsSchema);
//# sourceMappingURL=ArchiveReportProps.js.map