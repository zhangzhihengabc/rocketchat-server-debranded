export const convertToCss = (values, selector = ':root') => `${selector} {\n${Object.entries(values)
    .map(([name, color]) => `--rcx-color-${name}: ${color};`)
    .join('\n')}\n}`;
//# sourceMappingURL=convertToCss.js.map