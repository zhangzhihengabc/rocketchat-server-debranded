import React, { memo } from 'react';
import { createPortal } from 'react-dom';
import { convertToCss } from './helpers/convertToCss';
import { useCreateStyleContainer } from './hooks/useCreateStyleContainer';
import { darkPalette } from './paletteDark';
export const SidebarPaletteStyleTag = memo(function SidebarPaletteStyleTag() {
    const palette = convertToCss(Object.assign({}, darkPalette), '.rcx-sidebar--main');
    return React.createElement(React.Fragment, null, createPortal(palette, useCreateStyleContainer('sidebar-palette')));
});
//# sourceMappingURL=SidebarPaletteStyleTag.js.map