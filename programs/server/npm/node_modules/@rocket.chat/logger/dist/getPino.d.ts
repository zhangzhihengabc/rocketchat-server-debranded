import type { Logger } from 'pino';
import './logQueue';
declare function logMethod(this: Logger, args: unknown[], method: any): void;
declare const mainPino: Logger<{
    transport?: {
        target: string;
        options: {
            colorize: boolean;
        };
    } | undefined;
    hooks: {
        logMethod: typeof logMethod;
    };
    customLevels: {
        http: number;
        method: number;
        subscription: number;
        startup: number;
    };
    level: string;
    timestamp: () => string;
}>;
export type MainLogger = typeof mainPino;
export declare function getPino(name: string, level?: string): MainLogger;
export {};
