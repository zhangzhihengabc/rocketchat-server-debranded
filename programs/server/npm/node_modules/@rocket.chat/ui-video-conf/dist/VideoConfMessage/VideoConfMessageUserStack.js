import { jsx as _jsx } from "react/jsx-runtime";
import { Avatar, Box } from '@rocket.chat/fuselage';
import { useUserAvatarPath } from '@rocket.chat/ui-contexts';
import { memo } from 'react';
const MAX_USERS = 3;
const VideoConfMessageUserStack = ({ users }) => {
    const getUserAvatarPath = useUserAvatarPath();
    return (_jsx(Box, { mi: 4, children: _jsx(Box, { display: 'flex', alignItems: 'center', mi: 'neg-x2', children: users.slice(0, MAX_USERS).map(({ username }, index) => (_jsx(Box, { mi: 2, children: _jsx(Avatar, { size: 'x28', "data-tooltip": username, url: getUserAvatarPath(username) }) }, index))) }) }));
};
export default memo(VideoConfMessageUserStack);
//# sourceMappingURL=VideoConfMessageUserStack.js.map